import 'package:flutter/material.dart';

class BasicoPage extends StatelessWidget {

//Inicial
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Basico Page')
      ),
    );
  }
}